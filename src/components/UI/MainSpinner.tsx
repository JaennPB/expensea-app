import { View, Text } from "react-native";
import React from "react";

interface Props {}

const MainSpinner: React.FC<Props> = ({}) => {
  return (
    <View>
      <Text>MainSpinner</Text>
    </View>
  );
};

export default MainSpinner;
